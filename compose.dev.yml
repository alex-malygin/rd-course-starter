services:
  api:
    build:
      target: dev
    command: pnpm run start:dev
    ports:
      - '3000:3000'
    volumes:
      - .:/app
      - /app/node_modules
    networks:
      - public
      - internal

  postgres:
    ports:
      - '5432:5432'
